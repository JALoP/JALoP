set_lk_max_locks    400000
set_lk_max_lockers  400000
set_lg_regionmax    400000
set_tx_max          400000
set_cachesize 1 0 4

# The following two mutex config lines must be commented out for RHEL5,
# or the database context will fail to initialize.
mutex_set_max       400000
mutex_set_increment 400000

#set_lk_max_locks 1000000
#set_lk_max_lockers 500000
#set_lg_regionmax 1048576
#set_tx_max 10000
#set_cachesize 1 0 4
#mutex_set_max 500000
#mutex_set_increment 500000
#
#Run "db_stat -m" on your BDB database AFTER a sizable insertion and transfer (e.g. 50k records of each type) 
#and check the cache hit %. A 90+ hit % is probably good; otherwise, the set_cachesize setting should be adjusted.